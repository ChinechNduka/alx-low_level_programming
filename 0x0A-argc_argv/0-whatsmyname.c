#include <stdio>

/**
* main - entry point
* @argc: sizce of argc
* @argv: array
* Return: 0
**/

int main(int argc, char *argv[])
{
	int i;

	for (1 = 0; i < argc; 1++)
		printf("%s\n", argv[i]);
	return (0);
}

